version: '3.7'
services:
    postgres:
        image: postgres:10.5
        restart: always
        environment:
          - POSTGRES_PASSWORD=root
          - POSTGRES_USER=root
          - POSTGRES_DB=test
        ports:
          - '5432:5432'
        volumes:
    #Reel:#Virtuel
          - /null:/faux












